[switches]
# Add your switch IPs and credentials below
# switch1 = ip:port:username:password
# Example:
# core_switch = 192.168.1.1:22:admin:cisco123

[settings]
# How often to check logs (in minutes)
poll_interval = 60
# Log patterns to identify errors
error_patterns = ERROR,error,CRITICAL,critical,FATAL,fatal
